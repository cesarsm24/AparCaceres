# Dependencias directas del backend AparCáceres.
#
# Este fichero declara los paquetes de runtime mantenidos manualmente. El
# lockfile generado debe fijar también las dependencias transitivas con sus
# versiones exactas.

fastapi==0.136.1
uvicorn==0.46.0
pydantic==2.13.3
redis==7.4.0
python-dotenv==1.2.2
slowapi==0.1.9
prometheus-fastapi-instrumentator==7.0.0
PyJWT==2.10.1

# Cliente HTTP asíncrono usado por el importador para resolver fotos externas.
httpx>=0.27,<1